# 🚨 CRITICAL AI INSTRUCTIONS 🚨

## ⚠️ STRICT DATA PRESERVATION RULE ⚠️

**UNDER NO CIRCUMSTANCES SHOULD YOU DELETE, REMOVE, OR DESTROY ANY DATA.**

*   **Database Files:** Never delete files in `data/`, `data/db/`, or any mongodb data directories.
*   **Backups:** Never delete backup files in `backend/backups/` or `backup/`.
*   **Source Code:** Do not delete source files unless explicitly verifying it is safe and redundant (and even then, prefer renaming).
*   **Git Operations:** Be extremely careful with `git clean`, `git reset --hard`, or `git rm`. Always use `--cached` if un-tracking files to keep local copies.

**PRIORITY 1: DATA SAFETY. IF AN ACTION MIGHT RISK DATA LOSS, STOP AND ASK THE USER.**
