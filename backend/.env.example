# Database Configuration
# For Local MongoDB (development)
# MONGODB_URI=mongodb://localhost:27017/brc_transport

# For MongoDB Atlas (production/cloud)
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/brc_transport?retryWrites=true&w=majority
DB_NAME=brc_transport

# Server Configuration
PORT=5001
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production
JWT_EXPIRES_IN=7d

# CORS Configuration
FRONTEND_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,http://192.168.1.140:3000

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# MongoDB Atlas Setup Instructions:
# 1. Create account at https://www.mongodb.com/cloud/atlas
# 2. Create a new cluster (free M0 tier available)
# 3. Create database user with read/write permissions
# 4. Add your IP to network access (or 0.0.0.0/0 for anywhere)
# 5. Get connection string and replace the MONGODB_URI above
# 6. Replace 'username', 'password', and cluster details with your actual values
